import React from 'react'
import LoginScreen from '../src/screens/LoginScreen'

export default function Page() {
  // Renderizamos pantalla de login dentro del esquema de rutas de Expo Router.
  return <LoginScreen />
}
